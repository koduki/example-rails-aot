# Application dependencies are locked separately in bench/*Gemfile.lock.
BENCH_RAILS_VERSION=8.0.5.1
BENCH_RUBY_VERSION=3.4.5
BENCH_JRUBY_VERSION=10.0.7.0
BENCH_BUNDLER_VERSION=2.6.9
# Exact base digests are captured in the build manifest; refresh explicitly.
BENCH_CRUBY_IMAGE=ruby:3.4.5-bookworm
BENCH_JRUBY_IMAGE=jruby:10.0.7.0-jdk21
BENCH_NATIVE_IMAGE=ubuntu:24.04
